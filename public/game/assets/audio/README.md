These WAV files are generated placeholders.
Replace them with production assets using the same ids in `public/game/assets/manifest.json`:

- music: loopable BGM or ambience
- sfx: one-shot sound effects
- voice: one line per dialogue line, or use a later voice-bundle plugin

Recommended naming pattern:
`voice_<character>_<chapter>_<line-id>.wav`
